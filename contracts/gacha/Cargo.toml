[package]
name = "gacha"
version = "0.1.0"
edition = "2021"
description = "Gacha lootbox contract for Stellar"

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
soroban-sdk = "28.0.0"

[dev-dependencies]
soroban-sdk = { version = "28.0.0", features = ["testutils"] }
